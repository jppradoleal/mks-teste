name: CHECKS

on:
  push:
    branches:
      - "*"

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install Node.js
        uses: ./.github/actions/checks.action
      